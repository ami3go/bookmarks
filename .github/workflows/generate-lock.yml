name: Lockfile generator retired

# Temporary workflow intentionally disabled after package-lock.json was generated.
# Permanent validation lives in .github/workflows/ci.yml.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired"
